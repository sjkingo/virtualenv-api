language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"
install: 
    - pip install --upgrade pip
    - pip install -q six
script: python tests.py
branches:
    only:
        - master
sudo: false
addons:
    apt:
        packages:
            - pandoc
